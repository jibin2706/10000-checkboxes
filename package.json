{
    "name": "checkbox-sync",
    "version": "0.0.1",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "predeploy:client": "tar -cvf ./deploy.tar ./captain-definition-client ./packages/client/dist/*"
    }
}